<% distributor_authorities = Hyrax::DistributorAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a distributor',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Distributor</label>

<%= select_tag "distributor_authorities",
        options_for_select(distributor_authorities.select_active_options),
        class: 'form-control image_distributor',
        data: {
          'authority-select' => "image_distributor",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.distributor.prompt')
  %>
</fieldset>
