<% draftsman_authorities = Hyrax::DraftsmanAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a draftsman',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Draftsman</label>

<%= select_tag "draftsman_authorities",
        options_for_select(draftsman_authorities.select_active_options),
        class: 'form-control image_draftsman',
        data: {
          'authority-select' => "image_draftsman",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.draftsman.prompt')
  %>
</fieldset>
