<% compiler_authorities = Hyrax::CompilerAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a compiler',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Compiler</label>

<%= select_tag "compiler_authorities",
        options_for_select(compiler_authorities.select_active_options),
        class: 'form-control image_compiler',
        data: {
          'authority-select' => "image_compiler",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.compiler.prompt')
  %>
</fieldset>
