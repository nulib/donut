<% creator_authorities = Hyrax::CreatorAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a creator',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Creator</label>

<%= select_tag "creator_authorities",
        options_for_select(creator_authorities.select_active_options),
        class: 'form-control image_creator',
        data: {
          'authority-select' => "image_creator",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.creator.prompt')
  %>
</fieldset>
