<% director_authorities = Hyrax::DirectorAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a director',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Director</label>

<%= select_tag "director_authorities",
        options_for_select(director_authorities.select_active_options),
        class: 'form-control image_director',
        data: {
          'authority-select' => "image_director",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.director.prompt')
  %>
</fieldset>
