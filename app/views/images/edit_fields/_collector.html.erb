<% collector_authorities = Hyrax::CollectorAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a collector',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Collector</label>

<%= select_tag "collector_authorities",
        options_for_select(collector_authorities.select_active_options),
        class: 'form-control image_collector',
        data: {
          'authority-select' => "image_collector",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.collector.prompt')
  %>
</fieldset>
