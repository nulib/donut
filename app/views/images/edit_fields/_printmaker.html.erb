<% printmaker_authorities = Hyrax::PrintmakerAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a printmaker',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Printmaker</label>

<%= select_tag "printmaker_authorities",
        options_for_select(printmaker_authorities.select_active_options),
        class: 'form-control image_printmaker',
        data: {
          'authority-select' => "image_printmaker",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.printmaker.prompt')
  %>
</fieldset>
