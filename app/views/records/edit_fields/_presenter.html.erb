<% presenter_authorities = Hyrax::PresenterAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a presenter',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Presenter</label>

<%= select_tag "presenter_authorities",
        options_for_select(presenter_authorities.select_active_options),
        class: 'form-control image_presenter',
        data: {
          'authority-select' => "image_presenter",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.presenter.prompt')
  %>
</fieldset>
