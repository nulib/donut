<%= f.input key,
            as: :multi_value,
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/authorities/search/loc/languages",
                      'autocomplete' => key }
            },
            required: f.object.required?(key) %>
