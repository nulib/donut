<% cartographer_authorities = Hyrax::CartographerAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a cartographer',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Cartographer</label>

<%= select_tag "cartographer_authorities",
        options_for_select(cartographer_authorities.select_active_options),
        class: 'form-control image_cartographer',
        data: {
          'authority-select' => "image_cartographer",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.cartographer.prompt')
  %>
</fieldset>
