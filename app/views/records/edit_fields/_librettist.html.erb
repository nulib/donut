<% librettist_authorities = Hyrax::LibrettistAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a librettist',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Librettist</label>

<%= select_tag "librettist_authorities",
        options_for_select(librettist_authorities.select_active_options),
        class: 'form-control image_librettist',
        data: {
          'authority-select' => "image_librettist",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.librettist.prompt')
  %>
</fieldset>
