<% sculptor_authorities = Hyrax::SculptorAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a sculptor',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Sculptor</label>

<%= select_tag "sculptor_authorities",
        options_for_select(sculptor_authorities.select_active_options),
        class: 'form-control image_sculptor',
        data: {
          'authority-select' => "image_sculptor",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.sculptor.prompt')
  %>
</fieldset>
