<% artist_authorities = Hyrax::ArtistAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for an artist',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Artist</label>

<%= select_tag "artist_authorities",
        options_for_select(artist_authorities.select_active_options),
        class: 'form-control image_artist',
        data: {
          'authority-select' => "image_artist",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.artist.prompt')
  %>
</fieldset>
