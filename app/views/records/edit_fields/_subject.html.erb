<% subject_authorities = Hyrax::SubjectAuthorities.new %>

<%=
  f.input key,
  as: :multi_value,
  input_html: {
    class: 'form-control',
    data: { 'autocomplete-url' => "/authorities/search/loc/names",
      'autocomplete' => key }
  } ,
  required: f.object.required?(key) %>

 <%= label_tag('subject_authorities', t('hyrax.records.edit_fields.subject')) %>
 <%= select_tag "subject_authorities", options_for_select(subject_authorities.select_active_options), class: 'form-control image_subject', data: { 'authority-select' => "image_subject" }
  %>
