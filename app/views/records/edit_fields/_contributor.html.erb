<% contributor_authorities = Hyrax::ContributorAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a contributor',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Contributor</label>

<%= select_tag "contributor_authorities",
        options_for_select(contributor_authorities.select_active_options),
        class: 'form-control image_contributor',
        data: {
          'authority-select' => "image_contributor",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.contributor.prompt')
  %>
</fieldset>
