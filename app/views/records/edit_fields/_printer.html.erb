<% printer_authorities = Hyrax::PrinterAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for a printer',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Printer</label>

<%= select_tag "printer_authorities",
        options_for_select(printer_authorities.select_active_options),
        class: 'form-control image_printer',
        data: {
          'authority-select' => "image_printer",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.printer.prompt')
  %>
</fieldset>
