<%= f.input key,
            as: :controlled_vocabulary,
            placeholder: 'Search for a contributor role',
            input_html: {
              class: 'form-control',
              data: { 'autocomplete-url' => "/authorities/search/loc/relators",
                      'autocomplete' => key }
            },
            ### Required for the ControlledVocabulary javascript:
            wrapper_html: { data: { 'autocomplete-url' => "/authorities/search/loc/relators",
                                    'field-name' => key }},
            required: f.object.required?(key) %>
