<% author_authorities = Hyrax::AuthorAuthorities.new %>
<fieldset class="bordered form-group">
<%= f.input key,
  as: :controlled_vocabulary,
  placeholder: 'Search for an author',
  input_html: {
    class: 'form-control',
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'autocomplete' => key
    }
  },
  ### Required for the ControlledVocabulary javascript:
  wrapper_html: {
    data: {
      'autocomplete-url' => "/authorities/search/getty/ulan",
      'field-name' => key
    }
  },
  required: f.object.required?(key) %>

  <label>Authority for Author</label>

<%= select_tag "author_authorities",
        options_for_select(author_authorities.select_active_options),
        class: 'form-control image_author',
        data: {
          'authority-select' => "image_author",
          'uses-controlled-vocab' => true
        },
        prompt: t('hyrax.records.edit_fields.author.prompt')
  %>
</fieldset>
