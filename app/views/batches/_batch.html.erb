<tr class="<%= status_context batch.status %>">
  <td><%= status_label batch.status %></td>
  <td><%= link_to(batch.job_id, url_for(batch)) %></td>
  <td><%= batch.submitter %></td>
  <td><%= batch.original_filename %></td>
  <td><%= time_ago_in_words(batch.created_at) %> ago</td>
</tr>
