#!/bin/bash

if [[ $WORKER == 'true' ]]; then
  CMD="bundle exec shoryuken -R -C config/shoryuken.yml -q ${SETTINGS__AWS__QUEUES__INGEST:-donut.fifo}"
else
  CMD="bundle exec puma -C config/puma.rb"
fi
exec ${CMD}
